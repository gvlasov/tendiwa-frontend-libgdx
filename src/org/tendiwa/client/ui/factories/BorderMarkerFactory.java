package org.tendiwa.client.ui.factories;

import org.tendiwa.client.rendering.markers.BorderMarker;
import org.tendiwa.core.Border;

public interface BorderMarkerFactory {
public BorderMarker create(Border border);
}
